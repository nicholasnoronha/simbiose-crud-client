const Loading = () => {
    return <div style={{
        width: "100vw",
        height: "100vh",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
    }}>
        <img src="/loading-gif.gif" style={{width: "5rem"}}/>
    </div>
}

export default Loading